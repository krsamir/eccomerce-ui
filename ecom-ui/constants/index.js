import ROUTE_PATHS, { ROLES, ROLE_MAP, STORAGE_KEYS } from "./utilityConstants";

const CONSTANTS = Object.freeze({
  ROUTE_PATHS,
  ROLES,
  ROLE_MAP,
  STORAGE_KEYS,
});

export default CONSTANTS;

export { ROUTE_PATHS, ROLES, ROLE_MAP, STORAGE_KEYS };
